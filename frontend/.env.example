# Copy to .env.local for local overrides if ever needed (gitignored).
# Not required for local dev — the client falls back to http://localhost:8000.
# In production this is set in the Vercel dashboard (build-time variable):
VITE_API_URL=https://your-backend.example.com
